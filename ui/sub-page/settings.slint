import { Theme } from "../theme.slint";
import { TopBar } from "../components/topbar.slint";

export component SubPageSettings inherits VerticalLayout {
    TopBar {
        title: "Setting";
    }
}
