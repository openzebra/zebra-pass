
import { Button, VerticalBox } from "std-widgets.slint";

export component LockPage inherits Window {
  HorizontalLayout {
    VerticalLayout {
        Rectangle {
            width: 30%;
            background: green;
        }
    }
    VerticalLayout {
        Rectangle {
            background: #fff;
        }
        // Button {
        //     text: "test";
        // }
    }
  }
}
