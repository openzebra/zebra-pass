import { CheckBox } from "std-widgets.slint";
import { Theme } from "../theme.slint";
import { Routers } from "../routers.slint";
import { TopBar } from "../components/topbar.slint";
import { LeftBar } from "../components/leftbar.slint";


export component HomePage inherits Window {
    callback continue(Routers);

    HorizontalLayout {
        LeftBar {}
        VerticalLayout {
            height: Theme.app-height;
            TopBar {}
            Rectangle {
                width: Theme.app-width;
                height: Theme.app-height;
                background: #c29696;
            }
        }
    }
}
